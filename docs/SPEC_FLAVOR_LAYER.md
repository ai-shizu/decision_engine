# SPEC — LLM フレーバ層（Flavor Layer） v2

**正本。** 親: `docs/AI_SKILLS.md`（統治憲法）/ `docs/SPEC_BLACKBOX_SIMULATOR.md` §14（本機構の初出）。
不変条件は `FLV-I-nn`、罠は `FLV-W-nn` で採番する（`BXS-I-nn` / `BXS-W-nn` と同型・**改番禁止**）。

**v2 の変更点**: 裁定 FLV-R-6（提案 A′）により、フレーバ層は**数値を扱わなくなった**。数値検出器の役割は「許可リストとの照合」から「**あらゆる数値表現の絶対的遮断**」へ縮退している。併せて FLV-R-7 により、出力型を**コンテキスト・ウィットネス**として型封印する。

---

## 0. 裁定台帳

| ID | 論点 | 裁定 | 日付 |
|---|---|---|---|
| FLV-R-1 | 射程 | **BLACKBOX アリーナ 1 面（イベント文言等）のみ**。既存の「LLM が本体」の面（CONSULT / 面接対話 / ES 添削 / 講評本文）には適用しない | 2026-07-28 |
| FLV-R-2 | 永続化 | **しない。** リプレイ純度を最優先とし、UI 表示専用の使い捨てとする | 2026-07-28 |
| FLV-R-3 | モデル | F-3 で同梱 1.5B を用い、**破棄率を実測**してから判断 | 2026-07-28 |
| FLV-R-4 | 既定 | **既定 OFF（opt-in）** | 2026-07-28 |
| FLV-R-5 | 文体 | テンプレート単位の**厳格上限**。冗長な出力は破棄し固定テンプレートへ | 2026-07-28 |
| **FLV-R-6** | **数値の扱い（提案 A′）** | **フレーバ層は数値を一切扱わない。** LLM への入力に数値を渡さず（`SlotValue` は閉じた enum のみ）、出力に数値表現が現れた時点で破棄する。許可リスト照合という設計を**廃止**する | 2026-07-28 |
| **FLV-R-7** | **出力型の封印** | 出力は**コンテキスト・ウィットネス**とする。`Serialize` は実装するが **`Deserialize` は実装しない**。兄弟モジュールによる二鍵封印を用いる | 2026-07-28 |

### FLV-R-6 の根拠（脅威モデルの訂正）

当初は「敵対的生成器」を前提に、数値許可リストとの正準値照合を設計していた。しかし本系に敵対者は存在しない —— slot は決定論コアが生成し、モデルは**同梱 1.5B・オフライン・ローカル**で、ユーザー自身のゲームの装飾を生成する。第三者が出力を操る経路がない。

**現実の脅威は攻撃ではなく幻覚である。** そして Fact 側の数値は Fact 側の view model が描画する以上、**フレーバは数値を含む必要がそもそもない**。

入力に数値が無ければ、**出力に現れた数値はすべて発明**である。判定から曖昧性が消え、位取り解析・転写曖昧性・単位クラス・複合加算といった偽陰性リスクの温床が丸ごと消滅する。代償は「フレーバが数量を言い換えられない」ことだけであり、権威数値の隣に置く装飾としてはむしろ望ましい文体である。

---

## 1. 目的とスコープ

### 1.1 Fact と Flavor

| | **Fact（決定論コア）** | **Flavor（非決定論表現）** |
|---|---|---|
| 産出 | コードのみ。同一入力 → 同一出力 | LLM。同一入力でも揺らぐ |
| 内容 | 数値・状態・判定・スコア | **散文のみ。数値を含まない** |
| 権威 | あり（永続化・再注入・分析対象） | **なし**（表示限り・使い捨て） |
| 欠落時 | 動作不能 | **既定動作**（固定テンプレート） |
| 検証 | 契約テスト・リプレイ恒等性 | ウィットネス化 or 破棄 |

### 1.2 受入基準

> **フレーバ層を丸ごと削除しても、アプリは完全に使用可能であり、権威状態はバイト単位で同一でなければならない。**

これを満たさない設計は、名称が何であれフレーバ層ではない。

### 1.3 射程（FLV-R-1）

**含む**: 決定論的に確定した Fact に対し、LLM が**言い回しのみ**を与える経路。初回適用先は BLACKBOX アリーナのイベント文言。

**含まない**: CONSULT の応答、面接対話、ES 添削、講評本文。これらは **LLM が主機能そのもの**であり、LAW-19 / no-llm-authority で既に律されている。

> **FLV-W-01（最大の罠）**: 「LLM が化粧」の面と「LLM が本体」の面を混同すること。射程の拡大は必ず裁定を経ること。

---

## 2. 設計公理

| ID | 公理 | 根拠 |
|---|---|---|
| **A-1 削除可能性** | フレーバ層は削除可能。LLM 不在は劣化ではなく既定 | SPEC_BLACKBOX §14 |
| **A-2 数値ゼロ** | **入力に数値を渡さず、出力の数値表現を絶対遮断する。** 監査済み例外表以外の数値表現を含む出力は破棄（修正・再試行しない） | FLV-R-6 |
| **A-3 権威への非到達** | フレーバ出力は永続化されず、再注入されず、6D / profile / gap / tensor へ入らない | LAW-19 |
| **A-4 アンビエント** | UI もゲームも LLM を待たない。タイムアウトは既定への復帰であってエラーではない | SPEC_BLACKBOX §14 |
| **A-5 型による遮断** | 権威データ型はフレーバ層へ渡せない。渡せるのは `TemplateId` と閉じた enum の定性タグのみ | 壁 W-a / W-b |
| **A-6 リプレイ純度** | リプレイ・監査時にフレーバを再生成しない | 第八律 |
| **A-7 証明は型が運ぶ** | 検査済みであることは「関数を呼んだか」ではなく**型が持つ**。検査を経ない出力型は構築できない | FLV-R-7 |

**A-2 の作法**: ガード不通過時に LLM へ再生成させる設計を**禁ずる**。再試行は検証器の穴を探索する行為であり、通過してしまった歪んだ出力が最も危険である。**破棄 → 固定テンプレート**の一択。

---

## 3. 型パイプライン — コンテキスト・ウィットネス（FLV-R-7）

### 3.1 全体像

```
LLM 生出力(String)
   │
   ├─ UnverifiedFlavor          ← 検証の証明を持たない。任意文字列から構築可
   │      │
   │      └─ verify(&self, ctx: &FlavorPolicy) -> Option<VerifiedFlavor>
   │              （唯一の遷移。数値表現の絶対遮断ほか §4 の全規約を実行）
   ▼
VerifiedFlavor                  ← Serialize のみ。Deserialize を実装しない
   │
   └─ IPC (Tauri) ─▶ FE: 表示専用スロットへ描画（数値抽出は禁止）
```

**シリアライズは証明を伝送しない。証明を消去する。** 送信側が検査したという事実は、受信側にとって「我々の Rust が我々の IPC 経路で送ってきた」以上の意味を持たない。したがって FE 側の brand 型は**証明ではなく衛生マーカー**である（§3.5）。

### 3.2 入力の封印 — `SlotValue` は閉じた enum（FLV-R-6）

```
TemplateId : 閉じた enum（文字列 ID を受け付けない）
SlotId     : 閉じた enum
SlotValue  : 閉じた enum の定性タグのみ
             例) LossSeverity::High / Trend::Rising / Phase::Settlement
```

**`from_quantized` は廃止する。** 量子化数値を文字列化して渡す経路（SPEC_BLACKBOX §14 の当初定義）は本 SPEC が上書きする。

`SlotValue` が閉じた enum である以上、**封印機構は不要**である。型システムが既に任意値の混入を禁じている。権威型（`BlackboxProfile` / `TensorProfileReportV1` / `GenesisRequest` / vault row）に対して `Into<SlotValue>` を実装しないことが壁であり、実装されれば契約テストが落ちる。

> **FLV-W-02**: 「文脈を与えるため」に数値を prompt へ戻すこと。モデルに数を見せて「書くな」と指示する設計は弱い。**数を見せなければ、出力の数はすべて発明**という判定の単純さが失われる。

### 3.3 出力の封印 — 二鍵モジュール

`llm/flavor/` 配下に**兄弟モジュール 2 つ**を置く。Rust の可視性はモジュール単位であり、private 項目は定義モジュールとその子孫からのみ到達できる（親・兄弟からは不可）。

```
llm/flavor/
  ├─ checked.rs    pub(crate) struct Checked(String);   ← フィールドは checked に private
  │                pub(crate) fn check(raw, ctx) -> Option<Checked>
  ├─ verified.rs   pub struct VerifiedFlavor { checked: Checked }  ← フィールドは verified に private
  │                pub fn verify(raw, ctx) -> Option<VerifiedFlavor>
  │                impl Serialize for VerifiedFlavor（手書き・outbound 専用）
  ├─ wire.rs       pub struct UnverifiedFlavor(String);  ← Deserialize 可・証明なし
  ├─ policy.rs     pub struct FlavorPolicy（監査済み例外表 + 版番号）
  └─ mod.rs        再輸出のみ。`#![deny(unsafe_code)]`
```

**二鍵である理由**: `checked` は `Checked` を作れるが `VerifiedFlavor` を作れない。`verified` は `VerifiedFlavor` を作れるが `Checked` を偽造できない。したがって crate 内部の将来の編集であっても、`verify` を経ずにウィットネスを鋳造できない。単一モジュールの private では、同じファイルに `unchecked` コンストラクタを 1 行足すだけで封印が破れる。**本 crate は巨大な単一 crate であり、内部者脅威が現実的**であるため、二鍵は必須である。

**実測による検証（2026-07-28、リードアーキテクト）**: 最小再現を `rustc` で実コンパイルし、封印の成立を確認済み。

| 試験 | 結果 |
|---|---|
| 正常系（`verify` 経由） | コンパイル成功 |
| crate root から `VerifiedFlavor { checked }` を構築 | **`error[E0451]: field 'checked' of struct is private`** |
| 兄弟モジュールから `Checked(raw)` を偽造 | **`error[E0603]: tuple struct constructor 'Checked' is private`** |

### 3.4 実装してはならないもの

| 経路 | 措置 |
|---|---|
| `#[derive(Deserialize)]` on `VerifiedFlavor` | **禁止。** 私有フィールドを迂回して構築できる |
| `serde(from)` / `serde(try_from)` | 禁止（文脈なし変換を要求するため） |
| `Default` | 実装しない |
| `From<String>` / `TryFrom` / `FromStr` | 実装しない（`&FlavorPolicy` を受け取れない） |
| `Deref` / `DerefMut` / `AsMut` / `BorrowMut` | 実装しない。公開するのは `&str` のみ |
| `Clone` | **実装しない**（保守的既定）。複製可能な命題であることを文書化してから追加すること |
| `Arbitrary` / proptest 派生 | 依存自体を禁止 |
| `unsafe` / `transmute` | `llm/flavor/` に `#![deny(unsafe_code)]` を置く。外部の敵対的 `unsafe` には型で対抗できない（射程外） |

> **R1 の正確な表現**: 「唯一の構築経路」は厳密には成立しない。move・`identity`・std が定める反射的 `From<T> for T` は既存の値を複製する。**強制可能な命題は「未検証データから新しいウィットネスへの安全な遷移は `verify` のみ」である。**

### 3.5 FE 側（TypeScript）

- `VerifiedFlavor` は JSON 上ただの文字列として届く。TS 側の branded type は**証明ではなく衛生マーカー**であり、保証するのは「我々のパーサを通った」ことのみ
- **FE で再検証しない。** 監査済み例外表は Rust 側にあり、二重実装は乖離の温床になる。信頼は「我々の Rust が我々の IPC で送った」ことに依拠する（gptsol の指摘どおり、これは *proof* ではなく *trusted channel* である）
- フレーバ文字列を `Number()` / `parseFloat` / 数値抽出正規表現へ通す経路を**走査で禁止**
- `dangerouslySetInnerHTML` 禁止

> **FLV-W-03**: FE でフレーバから数値を再抽出すると、型の壁を迂回して非決定論的数値が権威側へ入る。

---

## 4. 数値表現の絶対遮断 — ガード規約

### 4.1 判定

```
ACCEPT iff
    文字種規約を満たす
    AND 長さが maxChars 以下
    AND 監査済み例外表の終端を除き、数値表現を一切含まない
```

不通過は**すべて破棄**。切り詰め・修正・再生成はしない（切り詰めは意味を変える）。

### 4.2 走査規約（G-1 成果の抽出）

1. **表示される正確な Unicode 文字列を検証する。** HTML 実体参照・Markdown・percent escape が後段で復号されるなら、**復号後に検証するか、そもそも禁止する**
2. **全文 NFKC を適用しない。** `㍿`→`株式会社`、`½`→`1⁄2` のように無関係な意味と byte span を破壊する。畳み込みは**承認した符号位置のみ**明示的に行う
3. **不可視・危険文字を拒否**: bidi 制御、zero-width、default-ignorable、異体字セレクタ、数値に隣接する予期しない結合文字
4. **最大数値島**を構成し、島を分割して小片ごとに判定しない（`2500円＋1円` 型のロンダリング防止）
5. **監査済み例外表は文法終端として照合する。部分文字列一致を使わない。** `万が一、一万円かかります` では `万が一` のみを遮蔽し、`一万円` は検出されなければならない
6. 拒否理由を見つけても**入力の最後まで走査**し、全所見を報告する（破棄率の計測に用いる）

### 4.3 検出しなければならない数値表現の族

半角/全角アラビア数字・科学記数法・漢数字（`〇一二…九`）・大字（`壱弐参拾`）・位取り（`二千五百`）・転写（`二〇二六`）・混在（`2千`, `1万5千`）・仮名数詞（`ゼロ`, `ふたり`）・外来数詞（`ダース`, `ハーフ`）・助数詞（`3人`, `三つ`）・序数（`第3`, `五番目`）・割合（`五割`, `三割五分`, `半分`）・比（`3対2`, `二倍`）・分数（`二分の一`）・通貨・期間・範囲（`3〜5`）・境界（`3以上`）・概数（`約3`, `十数`）・曖昧量（`数倍`, `大幅`）・相対変化（`倍増`, `半減`）。

**数詞を含まない数量表明**も遮断対象とする: `無料`（=0）、`無人`（=0）、`両名`（=2）、`ペア`（=2）、`ダース`（=12）。

> **FLV-W-04（`is_numeric()` の罠 — 実測済み）**: Rust の `char::is_numeric()` は General_Category **N** 系のみを真とする。実測結果:
>
> ```
> 一 二 十 壱 零 → cat=Lo  → is_numeric() == false  ← 取りこぼす
> 〇 → Nl,  Ⅳ → Nl,  ② → No,  ½ → No,  ٢ → Nd,  २ → Nd  → true
> ```
>
> **素朴に `is_numeric()` を使った実装は、`②` `Ⅳ` `½` `٢` `२` という珍しい数字を捕まえながら、日本語における主要な脅威である漢数字（`二千五百`）だけを静かに素通しさせる。** Rust std に Numeric_Type へのアクセサは無く、`to_digit()` も ASCII 系のみである。`is_numeric()` は**必要だが不十分な網**として扱い、**明示的な漢字・仮名テーブルと必ず併用すること。**

### 4.4 監査済み例外表

版番号付きの閉じた表とする。妥当な非数量終端の例: `一石二鳥` / `四苦八苦` / `三日坊主` / `万が一` / `十人十色` / `千差万別` / `四半期` / 接続表現の厳密形 `一方、` `一方で`。

**曖昧な部分文字列を包括的に免除してはならない**: `十分`（充分 / 十分間）、`一部`（一部数 / 一部分）、`五分五分`（慣用 / 約 50:50）、裸の `一方`、`十八番`、`二枚目`。これらは表に載せず、**含む出力は破棄**する（偽陽性は安価であり、固定テンプレートへ落ちるだけである）。

### 4.5 テストベクタの規約

**不可視文字をリテラルで書いてはならない。** 必ずエスケープで記述する（例: `"料金は2\u{200B}500円です。"`）。

> **FLV-W-05**: gptsol の G-1 解答では、U+200B を仕込んだ敵対的ベクタが**伝送中に不可視文字が脱落し、検証不能な状態**で提出された。リテラル記述は静かに壊れる。

---

## 5. 入出力規約

### 5.1 FlavorRequest（Rust → LLM）

```
schema:     "flavor_request.v1"
templateId: TemplateId                      （閉じた enum）
slots:      [{ id: SlotId, tag: SlotValue }]（閉じた enum の定性タグのみ・数値なし）
locale:     "ja"
maxChars:   u16                             （テンプレートごとの上限・FLV-R-5）
```

`#[serde(deny_unknown_fields)]`。fields=camelCase / variants=snake_case。

### 5.2 FlavorResponse（LLM → Rust）

**プレーンテキストのみ。LLM に JSON を書かせない。** フレーバは構造を持たず、JSON 化は「パース失敗」という失敗モードを増やすだけである。`llm/schema.rs` の構造化出力は**抽出タスク**の作法であり、フレーバには適用しない。

---

## 6. 失敗モードと縮退

| 事象 | 挙動 |
|---|---|
| モデル未ロード / feature 無効 | 固定テンプレート（**既定であり、エラーではない**） |
| タイムアウト | 固定テンプレート。UI にエラーを出さない |
| ガード不通過 | **破棄** → 固定テンプレート。再試行しない |
| LLM 例外 | 固定テンプレート。生エラーは `log::error!` のみ |

フレーバの失敗はユーザーにとって**非イベント**である。

**破棄率の内部計測は残す。** 高止まりは「モデルが指示に従えていない」ことの検出であり、FLV-R-3 の判断材料となる。計測値は権威状態ではない。

**補償制御（検出器に依存しない防御）**: フレーバを視覚的に区別して描画する（減光・イタリック・接頭記号等）。散文が計器の読みと誤読される経路を、表示層で断つ。

---

## 7. 決定論の保全（FLV-R-2）

- リプレイ時にフレーバを**再生成しない**
- フレーバ文字列を決定ログに**混ぜない**。混ぜれば `state_digest` が非決定論に汚染され、Phase 6-A のリプレイ恒等性（`ReplayDivergence` による書込拒否）が意味を失う
- **永続化しない。** vault のどのテーブルにもフレーバ列を作らない

---

## 8. ウィットネスが証明しないもの

`VerifiedFlavor` が証明するのは次の一点のみである。

> 保持している文字列は、`verify` に渡された `FlavorPolicy` によって、その時点で受理された。

証明しないもの: 渡された policy が正統であったこと / policy の版 / 変更・失効・時間経過後も妥当であること / 検証実装が正しいこと / 送信者の真正性 / 送信者が実際に検証を行ったこと / 外部の敵対的 `unsafe` に対する安全性。

**「verified」という語を偽にしないため、文脈なしの逆シリアライズを実装してはならない。**

---

## 9. 不変条件台帳

| ID | 内容 | ガード（実装時に実名へ） |
|---|---|---|
| FLV-I-01 | フレーバ層削除でも全機能が動作 | feature 無効構成での全テスト GREEN |
| FLV-I-02 | `SlotValue` は閉じた enum であり、数値・任意文字列を保持しない | 契約テスト走査 |
| FLV-I-03 | 権威型に `Into<SlotValue>` が存在しない | 契約テスト走査 |
| FLV-I-04 | `VerifiedFlavor` の構築経路が `verify` のみ（二鍵封印） | compile-fail + cfg プローブ |
| FLV-I-05 | FE がフレーバ文字列から数値を抽出しない | 契約テスト走査 |
| FLV-I-06 | フレーバが決定ログ / 権威テーブル / vault に入らない | 契約テスト + スキーマ走査 |
| FLV-I-07 | ガード不通過時に再試行しない | 契約テスト走査 |
| FLV-I-08 | `blackbox-sim` は `pocket-brain` に依存しない | Cargo.toml 契約テスト |
| **FLV-I-09** | `VerifiedFlavor` が `Deserialize` / `Default` / `Clone` / `DerefMut` を実装しない | compile-fail（負のトレイト境界） |
| **FLV-I-10** | FlavorRequest に数値が含まれない | 契約テスト走査 |
| **FLV-I-11** | フレーバは視覚的に区別された表示スロットにのみ描画される | FE 契約テスト |

### 9.1 封印テストの作法（実測に基づく必須規約）

> **FLV-W-06（rustdoc の `compile_fail` は理由を検証しない — 実測済み）**: `compile_fail,E0451` の**エラーコード注釈は強制されない**。実測では、実際のエラーが E0425（未定義識別子）でも、存在しない crate の import でも、rustdoc は「ok」と判定した。
>
> **したがって封印テストは、型名の変更・import ミス・タイプミスによって「間違った理由で通過」し得る。** 対策を必須とする:
>
> 1. **負のフェンスには必ず正の伴走テストを置く。** 同一の import・同一の scaffolding で、攻撃部分だけを取り除いたコードが**コンパイルできること**を確認する。これにより「周辺コードが壊れているせいで落ちている」状態を検出する
> 2. **攻撃 1 つにつきフェンス 1 つ。** 複数の欠如を 1 フェンスにまとめると、残った 1 つのエラーが新しく開いた穴を隠す
> 3. **crate 内部の封印（private → `pub(crate)` への退行）は doctest では検出できない。** `#[cfg(...)]` プローブを本体に置き、**stderr に期待するエラーコードが現れることを assert する**ハーネスで駆動する（これは理由まで検証できる唯一の形）

### 9.2 本 crate 固有の注意

gptsol の `must_fail` ハーネスは `cargo rustc --cfg ...` で lib を再コンパイルする。**本 crate は巨大な単一 Tauri crate であり、`cargo test` の内部から入れ子で cargo を起動すると、再コンパイル時間と lock 競合の双方で不安定になる。** 内部封印プローブは `cargo test` の中ではなく、**CI の独立したステップ**（シェル）として実行すること。

---

## 10. フェーズ計画

| Phase | 内容 | 完了条件 |
|---|---|---|
| **F-1** | 型骨格のみ。`TemplateId` / `SlotId` / `SlotValue`（閉じた enum）、`checked` / `verified` 二鍵モジュール、`UnverifiedFlavor`、`FlavorPolicy` の器。**LLM 呼び出しなし・ガードは常に None を返す骨格** | FLV-I-02〜04, I-09 GREEN。封印プローブが E0451 / E0603 で落ちることを実測 |
| **F-2** | ガード完全実装（§4 の全規約 + 監査済み例外表）+ 既知解テストベクタ | 数値表現族の検出率実測。偽陽性率も報告 |
| **F-3** | BLACKBOX アリーナ 1 面へ配線（アンビエント配信）。同梱 1.5B で**破棄率を実測** | FLV-I-01 GREEN・破棄率報告 → FLV-R-3 の判断 |
| **F-4** | 他面への展開 | 面ごとに裁定 |

**F-1 と F-2 を分ける理由**: 型の封印は LLM が 1 行も動かなくても検証できる。**防壁を先に建て、非決定論は後から入れる。**

---

## 付録 A. 特務機関（gptsol）成果の採否

| 成果 | 採否 |
|---|---|
| G-1: 数値表現の分類表（数詞を含まない数量表明を含む） | **採用** — §4.3 |
| G-1: 走査規律（NFKC 禁止・不可視文字拒否・最大数値島・慣用句は文法終端） | **採用** — §4.2 |
| G-1: 正準化（Rational / UnitClass）・位取り解析・割合/比/期間の算術 | **不採用** — FLV-R-6 により不要 |
| G-1: 監査済み**文法**（sentence grammar）による入力空間の制限 | **不採用** — 脅威モデル訂正により過剰。フレーバ層の存在意義を消す |
| G-2: 非対称設計（Serialize あり / Deserialize なし） | **採用** — §3.1, §3.4 |
| G-2: 二鍵モジュール封印 | **採用** — §3.3（実測で成立を確認） |
| G-2: `Unverified` 別型による受信経路 | **採用** — §3.1 |
| G-2: `DeserializeSeed` アダプタ | **不採用** — 本系に受信側の再検証経路が不要（FE は表示のみ） |
| G-2: `Clone` を実装しない保守的既定 | **採用** — §3.4 |
| G-2: 「ウィットネスが証明しないもの」 | **採用** — §8 |
| G-2: rustdoc `compile_fail` によるテスト | **条件付き採用** — エラーコード注釈が強制されない実測に基づき、正の伴走テストを必須化（§9.1） |
